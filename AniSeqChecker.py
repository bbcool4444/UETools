import unreal


# access anim sequence data
def accessAnimSequenceData():
    # get the anim sequence
    anim_sequence = unreal.find_asset_by_class("AnimSequence")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("raw_animation_data")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("additive_base_pose")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("additive_base_reference_pose")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("additive_anim_type")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("additive_anim_type")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_interpolation")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_length")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_name")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    # get the animation data
    animation_data = anim_sequence.get_editor_property("animation_track_names")
    
    
if __name__ == '__main__':
    accessAnimSequenceData()